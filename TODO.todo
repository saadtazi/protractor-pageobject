TODOs:
  
  features:
    ☐ add setFields({elName: value, ...}) method:
        * if method `setElName` exist, use it
        * otherwise, detect the input field type and use sendKeys or other
    ☐ add ItemList class

  add tests:
    ☐ unit
    ☐ e2e

  prepare release:
    ☐ plug saucelabs and ci (travis)